# SSH Manager
Cli SSH connection manager

# Usage
On first launch the password you enter will be the key to encrypt and decrypt

# Run with any of these
- ```python3 ssh_man.py``` (After installing dependencies from [requirements.txt](/requirements.txt))
- ```bash ssh_man.sh```
- ```./ssh_man``` (This one is made to be symlinked)
